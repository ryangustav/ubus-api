/**
 * run-bootstrap.ts is obsolete with MongoDB since Mongoose creates collections automatically
 * and NestJS handles indexes via autoIndex (default: true).
 */
console.log('No bootstrap script needed for MongoDB. Mongoose handles scheme building and index creation!');
